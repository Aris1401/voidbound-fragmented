// maki.config.js — developer's single config file
export default {
  width: 900,   // game canvas width in px — editor canvas matches this
  height: 600,  // game canvas height in px — editor canvas matches this
  maps: ['realm_starting_area'],  // list of map names to load (without .json extension)
  sprites: ['lia'],
  debug: false
}
